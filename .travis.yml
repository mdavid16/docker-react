language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mdavid16/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mdavid16/docker-react npm run test
